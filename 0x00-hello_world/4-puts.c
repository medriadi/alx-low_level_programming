#include <stdio.h>

/**
 * main - Entry point of the program, displays a message.
 *
 * Return: Always returns 0 to exit the program (Success).
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
